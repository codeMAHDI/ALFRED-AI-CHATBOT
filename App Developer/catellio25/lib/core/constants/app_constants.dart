class AppConstants {
  static const String appName = 'Catellio25';
  
  // Add other global constants here (e.g., API base URL, timeout durations, etc.)
  static const Duration splashDuration = Duration(seconds: 3);
}
